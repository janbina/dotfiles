- defaults:
    link:
      create: true
      relink: true

- plugins:
    - dotbot-brew/

- link:
    ~/.config/fish:
    ~/.config/git:
    ~/.config/skhd:
    ~/.config/starship.toml: starship/starship.toml
    ~/.config/yabai:
