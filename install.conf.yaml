- defaults:
    link:
      create: true
      relink: true

- link:
    ~/.config/fish:
    ~/.config/git:
    ~/.config/skhd:
    ~/.config/starship.toml: starship/starship.toml
    ~/.config/yabai:
    ~/.local/share/fish/fish_history:
    ~/.ssh: ssh
    ~/.vimrc: vimrc

