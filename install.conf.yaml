- defaults:
    link:
      create: true
      relink: true

- link:
    ~/.config/git:
    ~/.config/polybar:
    ~/.config/rofi:
    ~/.config/redshift:
    ~/.config/starship.toml: starship/starship.toml
    ~/.config/swm:
    ~/.config/sxhkd:

